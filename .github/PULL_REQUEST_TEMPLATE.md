## What does this PR do?

<!-- e.g. "Adds a new `qed` snippet for closing proofs" -->

## Checklist

- [ ] Tested the snippet(s) in Vim/Neovim with UltiSnips before submitting
- [ ] Trigger doesn't collide with an existing one (see the table in README.md)
- [ ] Added/updated a row in the README snippet table for any new or changed trigger
- [ ] Followed the existing format: `snippet <trigger> "<description>" <flags>` ... `endsnippet`
- [ ] No trailing whitespace on any line

An automated check will run on this PR and comment with specific feedback if
anything above needs fixing — no need to guess, just watch for that comment.
